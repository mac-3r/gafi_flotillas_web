<%= form_with(model: @concept,url: create_new_concept_path, local: true) do |form| %>
  <% if concept.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(concept.errors.count, "error") %>Error al registrar:</h2>

      <ul>
        <% concept.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="float-right">
  <%= form.submit "Guardar", class: "btn btn-success", style:"background-color: #2dbe06 !important;" %>
  </div>
  <br>
  <br>

  <div class="form-group row">
    <label class="col-sm-2 col-form-label" style="font-family: Arial; font-size:16px;">Descripción:</label>
    <div class="col-sm-0">
      <%= form.text_field :descripcion, class: "form-control", style:"width: 250px !important;" %>
    </div>
  </div>
  
<% end %>