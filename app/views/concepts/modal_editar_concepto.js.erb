<% if @concepto%>
    $("#titulo_modal_general").text("Editar concepto de mantenimiento <%= @concepto.nombre %>");
    $(".modal_general").modal("show");
    $("#contenido").html("<%= escape_javascript(render partial: 'form_conceptos') %>");
<% else %>
    toastr.options = {
        "closeButton": false,
        "debug": false,
        "newestOnTop": false,
        "progressBar": false,
        "positionClass": "toast-top-right",
        "preventDuplicates": false,
        "onclick": null,
        "showDuration": "300",
        "hideDuration": "1000",
        "timeOut": "5000",
        "extendedTimeOut": "1000",
        "showEasing": "swing",
        "hideEasing": "linear",
        "showMethod": "fadeIn",
        "hideMethod": "fadeOut"
    };
    toastr.info("No se encontró el concepto seleccionado.");
<% end %>