<%= form_with(model: test_vehicle, local: true) do |form| %>
  <% if test_vehicle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_vehicle.errors.count, "error") %> prohibited this test_vehicle from being saved:</h2>

      <ul>
        <% test_vehicle.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :numero %>
    <%= form.number_field :numero %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
